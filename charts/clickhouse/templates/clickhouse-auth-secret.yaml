{{- if and .Values.clickhouse.auth.enabled .Values.clickhouse.auth.createSecret }}
{{- $username := .Values.clickhouse.auth.username }}
{{- $password := .Values.clickhouse.auth.password }}
{{- if and $username $password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clickhouse.authSecretName" . }}
  labels:
    {{- include "clickhouse.labels" . | nindent 4 }}
    app.kubernetes.io/component: clickhouse
type: Opaque
stringData:
  username: {{ $username | quote }}
  password: {{ $password | quote }}
{{- else }}
{{- fail "Username and password must be provided when ClickHouse auth is enabled and createSecret is true" }}
{{- end }}
{{- end }}